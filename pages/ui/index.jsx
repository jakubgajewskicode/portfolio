import Head from 'next/head';
import CustomUI from '../../components/UI';

const UI = () => (
  <>
    <Head>
      <title>UI</title>
    </Head>
    <div>
      <CustomUI />
    </div>
  </>
);

export default UI;
